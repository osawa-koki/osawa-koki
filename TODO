$ トリックアート(スリットアニメーション)

2枚のギザギザした画像をいい感じに重ね合わせてトリックアートを作る。

$ サブスク管理

サブスクをローカルストレージを使用して管理する。

$ 画像処理サイト

簡単な画像処理をするサイト。

$ simple-grep

ドラッグされたディレクトリを対象にgrep検索、置換を行う。

$ text-similarity-checker

テキストの類似度をチェックする。
